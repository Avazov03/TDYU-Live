export function muxPlayerUrl(playbackId: string) {
  return `https://player.mux.com/${playbackId}`;
}

/** OBS Server URL. Stream key dars boshlangach chiqadi. */
export const MUX_RTMP_URL = "rtmps://global-live.mux.com:443/app";

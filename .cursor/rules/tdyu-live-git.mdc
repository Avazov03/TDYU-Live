---
description: Lexify GitHub — commit va manba/main push faqat foydalanuvchi ruxsat bersa
alwaysApply: true
---

# Lexify — Git

Repo: `git@github.com:Avazov03/TDYU-Live.git`  
Remote: **manba** (origin emas). Branch: **main**.

Kod o‘zgagach agent **o‘zi** commit/push qilmasin. Avval so‘rasin. Faqat foydalanuvchi ruxsat bersa:

```
git push -u manba main
```

`.env` va `.secrets/` commit qilinmasin.
